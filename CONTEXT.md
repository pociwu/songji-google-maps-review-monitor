# 領域詞彙表

## 店家（Shop）

一個由名稱、Google Maps URL 與啟用狀態組成的監控對象。店家 URL 的雜湊值是系統內的穩定識別鍵。

## 評論快照（Review Snapshot）

某次監控讀取到的一則 Google Maps 評論完整狀態，包含作者、星等、內容、顯示時間、圖片與店家回覆等資訊。

## 監控檢查（Check）

一次對所有已啟用店家執行的抓取與比對工作。首次以 `init` 執行的檢查只建立基準；後續以 `check` 執行的檢查可產生通知事件。

## 通知事件（Notification Event）

評論快照相對於已保存狀態的可通知變化，或店家監控狀態的變化。例如新評論、評論更新、店家回覆變動、連續失敗及恢復成功。

## 通知分段（Notification Part）

一個通知事件送往 Telegram 的最小傳送單位。文字與媒體可能拆成多個分段；每個分段各自記錄傳送狀態與重試時間。

## 公開展示頁（Public Portal）

不需要應用程式登入即可瀏覽的動態網頁服務。它直接讀取監控程式使用的 SQLite 資料庫與媒體檔案，依店家整理已保存的評論、評論附帶照片，以及由管理者維護的內容素材；服務綁定於 `0.0.0.0:8088`，只透過 Tailscale tailnet 提供存取。

展示頁採手機優先的響應式介面，支援透過 Tailscale 存取的手機、平板與桌面裝置。

首頁以店家卡片列出所有店，顯示店名、監控健康度、最新評論時間與最新動態摘要；使用者可從卡片進入該店的完整展示頁。

店家完整頁依序呈現監控狀況、最新動態與所有評論。內容素材依發布時間倒序顯示；評論先顯示最新 20 筆，並可載入更多。

## 內容素材（Content Asset）

由管理者為特定店家建立的貼文、照片或影片連結；以伺服器檔案及 YAML 或 JSON 中繼資料管理，而不是透過網站後台。它不是 Google Maps 評論快照的一部分。

內容素材採統一的時間流模型：一筆素材可以包含文字、多張照片，以及影片檔或影片連結。店家展示頁可將其分別呈現在「最新動態」與「媒體」等區塊。

影片可由伺服器上的影片檔或外部影片連結提供；兩種來源皆以內容素材中繼資料指定。

內容素材的中繼資料集中存放於一份 YAML 檔；照片與影片依店家資料夾保存。公開展示頁讀取該 YAML 檔以產生內容時間流。

人工素材的固定根目錄為 `data/portal/`：中繼資料為 `data/portal/content.yaml`，每店的本機媒體為 `data/portal/<店家代號>/`。此目錄與監控程式下載的評論媒體分離。

每筆內容素材至少具有唯一 `id`、店家代號 `shop_key`、發布時間 `published_at`、標題 `title` 與內文 `body`；可選擇提供照片清單及影片來源。

## 自動公開（Automatic Publication）

公開展示頁的展示政策：所有已保存的評論快照及其評論附帶照片，不需人工審核即可在公開展示頁顯示。

## 監控健康度（Monitoring Health）

公開展示頁為每一間店顯示的監控狀況：啟用狀態、上次成功查詢時間、最近一次結果、連續失敗次數與最新評論時間。它不得揭露 Google 帳號資料或系統內部錯誤細節。

## 評論卡片（Review Card）

公開展示頁中呈現一筆評論快照的元件。它顯示評者名稱、個人檔案連結、頭像、星等、評論時間、評論文字、店家回覆與評論附帶照片。
